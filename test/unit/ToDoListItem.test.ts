describe('Unit tests: list item', () => {
  it('Should add a list item', () => {
    expect(true).toBe(false);
  });
  it('Should persist a list item', () => {
    expect(true).toBe(false);
  });
  it('Should remove a list item', () => {
    expect(true).toBe(false);
  });
  it('Should remove persisted list item', () => {
    expect(true).toBe(false);
  });
  it('Should edit a list item', () => {
    expect(true).toBe(false);
  });
});